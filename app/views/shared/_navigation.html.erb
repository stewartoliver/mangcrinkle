<!-- Navigation -->
<nav class="bg-white shadow">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <div class="flex">
        <div class="flex-shrink-0 flex items-center">
          <%= link_to root_path, class: "text-2xl font-bold text-orange-950" do %>
            MangCrinkle
          <% end %>
        </div>
        <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
          <%= link_to "Home", root_path, class: "inline-flex items-center px-1 pt-1 text-sm font-medium text-orange-600 hover:text-orange-950" %>
          <%= link_to "Products", products_path, class: "inline-flex items-center px-1 pt-1 text-sm font-medium text-orange-600 hover:text-orange-950" %>
          <%= link_to "About", about_path, class: "inline-flex items-center px-1 pt-1 text-sm font-medium text-orange-600 hover:text-orange-950" %>
          <%= link_to "Contact", contact_path, class: "inline-flex items-center px-1 pt-1 text-sm font-medium text-orange-600 hover:text-orange-950" %>
        </div>
      </div>
      <div class="flex items-center">
        <%= render "shared/cart_icon" %>
        <% if user_signed_in? %>
          <div class="ml-4">
            <%= link_to "Account", edit_admin_user_path(current_user), class: "text-orange-600 hover:text-orange-950" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</nav> 